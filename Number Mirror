About the problem : So in this problem we have to the following steps:
1.take an input as an number
2.output the exact the number on output screen.


#include <iostream>
using namespace std;

int main() {
	int a; // declaration of variable a
  
	cin>>a; // taking the value of a as input
  
	cout<<a; //showing the output : the value of a
  
	return 0;
}
